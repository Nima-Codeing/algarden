services:
  postgres:
    image: postgres:16
    container_name: algarden-db
    environment:
      POSTGRES_USER: algarden
      POSTGRES_PASSWORD: algarden
      POSTGRES_DB: algarden
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
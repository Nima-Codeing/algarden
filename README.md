# AlGarden

> 探索アルゴリズムの実行過程を、SVG で育つ「庭」として可視化する Web アプリケーション

## 概要

AlGarden は、TODO を完了するたびにノードネットワークが成長していく、習慣化のための Web アプリケーションです。黒い宇宙空間（庭）に植えた「種」が、タスクの達成に応じて独自のアルゴリズム（Tendril Algorithm）のルールで枝分かれし、ネットワークとして育っていきます。
目標時間と実績の差分からスコアを算出し、達成度に応じて伸びるノード数が変化します。さらに低確率の「変異」によって、同じ種を育ててもほぼ同じ形にならない庭が生まれることを狙いとしています。
本プロジェクトは、モダンな技術スタックでの開発力と技術選定における意思決定の過程を示すことを目的に、個人開発のポートフォリオとして制作しています。

## 主要機能（現時点で画面から使えるもの）

現時点で実装・動作確認済みの機能は以下のとおりです。

ユーザー登録 / ログイン — メールアドレスとパスワードによるアカウント登録・認証。
Cookie ベースの JWT 認証 — JWT を HttpOnly Cookie に保持し、クロスドメイン構成下でセッションを維持。
認証状態の保持 — /auth/me による認証状態の復元で、リロード後もログイン状態を維持。
ルート保護 — 未ログイン時は保護ページにアクセスできない ProtectedRoute。


## 開発中 / 今後の予定（Roadmap）

 TODO 管理 — 登録・完了・タイマー・目標時間・スコアリング（バックエンド API 実装済み / 画面は実装中）
 Seed（種） — 種の取得・配置（バックエンド API 実装中）
 Garden（庭） — 期間ごとの庭の管理、期間リセット
 Tendril Algorithm（コア） — DFS ベースの蔓状成長ロジック
 変異システム — 小島・正八面体・放射の3種を設計図方式で生成
 SVG / Framer Motion 描画 — ノードネットワークの可視化、ビューポート仮想化
 アルゴリズム View — 成長の軌跡を再生するアニメーション、パン / ズーム操作
 デプロイ — Vercel + Railway

## 技術スタック

| 領域 | 技術 |
|------|------|
| フロントエンド | Vite, React, TypeScript|
| ルーティング | React Router |
| 状態管理 | Zustand |
| スタイリング | Tailwind CSS |
| アニメーション / 描画 | SVG, Framer Motion（導入予定） |
| バックエンド | NestJS, TypeScript |
| 認証 | JWT (Passport) + HttpOnly Cookie |
| ORM | Prisma 7 |
| DB | PostgreSQL（開発: Docker / 本番: Supabase） |
| モノレポ | Turborepo |
| インフラ | Vercel (フロント), Railway (バック) |

## 技術選定の理由（抜粋）

### Vite + React
このアプリの主な機能は、ノードネットワークが動いて育つアニメーションです。ログイン後に使うアプリなので検索エンジン対策（SEO）は不要で、Next.js のような SSR フレームワークの強みが活きません。それよりもゲーミフィケーションの観点から、画面の動きの滑らかさを優先したかったため、軽量な Vite + React を選びました。将来、庭をシェアする機能などで SEO が必要になったら Next.js への移行を検討します。

### Turborepo
フロントエンドとバックエンドで、データの「型」を共通で使いたかったため、両方を1つのリポジトリにまとめる Turborepo を採用しました。型を1か所（packages/shared）にまとめておくことで、「バックの API が返すデータの形」と「フロントが受け取る形」がズレるのを防げます。片方を直したらもう片方も直す必要がある、という抜け漏れをビルド時に気づける状態を作っています。

## アーキテクチャ

Turborepo によるモノレポ構成で、フロントエンドとバックエンドを単一リポジトリ内で管理している。

```
algarden/                  # Turborepo モノレポ
├── apps/
│   ├── web/               # Vite + React フロントエンド → Vercel
│   └── api/               # NestJS バックエンド → Railway
├── packages/              # 共有パッケージ（型定義など）
└── turbo.json
```

リクエストの流れは以下の通り。フロントとバックが別ドメインに配置されるため、認証は Cookie + CORS のクロスドメイン構成で成立させている。

```
[ Browser ]
    │  HttpOnly Cookie (JWT) / credentials: true / SameSite=Lax
    ▼
[ Vercel: React SPA ] ──── API リクエスト (CORS) ────▶ [ Railway: NestJS ]
                                                          │
                                                          ▼  Prisma 7
                                                     [ PostgreSQL ]
```

## ローカルでの起動方法
後々追記

## ライセンス
このプロジェクトは [MIT License](./LICENSE) の下で公開されている。